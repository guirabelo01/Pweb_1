<!doctype html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>NASA APOD Explorer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
    <style>
      body { background-color: #0b0c10; color: #c5c6c7; min-height: 100vh; }
      .navbar { background-color: #1f2833 !important; }
      .navbar-brand { color: #66fcf1 !important; font-weight: 700; }
      .btn-buscar { background-color: #45a29e; border: none; color: #0b0c10; font-weight: 700; }
      .btn-buscar:hover { background-color: #66fcf1; color: #0b0c10; }
      .card-img-carousel { height: 480px; object-fit: cover; }
      .carousel-caption { background: rgba(0,0,0,0.6); border-radius: 0.5rem; padding: 1rem; }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-dark">
      <div class="container">
        <span class="navbar-brand mb-0 h1">🔭 NASA APOD Explorer</span>
      </div>
    </nav>